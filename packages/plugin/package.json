{
  "name": "@cowork-tasks/plugin",
  "version": "0.1.0",
  "description": "Claude Cowork plugin manifest, skills, agents, and bundled MCP server for Cowork Tasks. Reads from Cowork-native MCP connectors declared in .mcp.json.",
  "license": "MIT",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node ./scripts/build-bundles.mjs && node ./scripts/sync-artifact.mjs && node ./scripts/validate-skills.mjs",
    "test": "vitest run --passWithNoTests",
    "typecheck": "echo '(no TS to check; plugin is JS + JSON)'"
  },
  "dependencies": {
    "@cowork-tasks/core": "workspace:*",
    "@cowork-tasks/mcp-server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "esbuild": "^0.21.0",
    "typescript": "^5.4.0"
  }
}
